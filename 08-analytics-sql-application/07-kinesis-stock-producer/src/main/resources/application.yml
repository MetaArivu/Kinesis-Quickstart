spring:
  application:
    name: kinesis-stock-producer

cloud:
  aws:
    inputstreamname: stock-input-stream
    credentials:
      accessKey: 
      secretKey: 
    region: ap-south-1
    stack:
      auto: false

