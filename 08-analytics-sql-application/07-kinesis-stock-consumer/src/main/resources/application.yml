
spring:
  cloud:
    stream:
      bindings:
        input:
          destination: stock-output-stream
          group: stock-output-stream-group
          content-type: text/plain

cloud:
  aws:
    credentials:
      accessKey: 
      secretKey: 
    region:
      static: ap-south-1
    stack:
      auto: false
